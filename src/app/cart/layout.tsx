export const metadata = {
  title: "Your Shopping Cart - Curio",
  description:
    "Review the items in your shopping cart and proceed to checkout. Curio makes shopping easy and seamless!",
};

const Layout = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  return <>{children}</>;
};

export default Layout;
