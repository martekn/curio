export const metadata = {
  title: "Contact Us - Curio",
  description:
    "Have questions or need assistance? Contact Curio today. Fill out our contact form for a prompt response.",
};

const Layout = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  return <>{children}</>;
};

export default Layout;
