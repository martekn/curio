export const metadata = {
  title: "Products - Curio",
  description:
    "Discover our wide range of products available at Curio. Find something that matches your style and taste.",
};

const Layout = ({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) => {
  return <>{children}</>;
};

export default Layout;
